name: Claude Mention Support

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  claude:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - name: Run Claude on mention
        uses: anthropics/claude-code-action@v1
